import Web3 from 'web3'

// value is either a return value from a call or an input value for a call
export const useFormatter = (value: any, retVal: boolean) => {
  console.log('value', value)

  return value
}
